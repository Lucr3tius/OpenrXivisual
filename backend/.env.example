# =============================================================
# arXivisual Backend — Environment Variables
# =============================================================
# Copy this file to .env and fill in your keys:
#   cp .env.example .env
# =============================================================

# ---------------------------
# LLM / AI
# ---------------------------

# ---------------------------
# Dedalus SDK + MCP Gateway
# ---------------------------
# REQUIRED: LLM calls are Dedalus-only in this project.
# The official Dedalus SDK (dedalus-labs) reads this env var automatically.
# Sign up at https://www.dedaluslabs.ai/dashboard/api-keys to get your key.
DEDALUS_API_KEY=dsk-your-dedalus-api-key

# Section formatter / parser LLM provider:
# - anthropic (default): direct Anthropic API calls for parsing/summarization
# - dedalus: route formatter calls through Dedalus as well
SECTION_FORMATTER_PROVIDER=anthropic

# Direct Anthropic key (required when SECTION_FORMATTER_PROVIDER=anthropic)
ANTHROPIC_API_KEY=sk-ant-...

# ---------------------------
# Storage Mode
# ---------------------------
# "local" (default) — files stored in ./media/videos/
# "r2" — Cloudflare R2 cloud storage
STORAGE_MODE=local

# ---------------------------
# Cloudflare R2 (only needed when STORAGE_MODE=r2)
# ---------------------------
# S3_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
# S3_BUCKET=arxiviz-videos
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
# S3_PUBLIC_URL=https://videos.arxiviz.org

# ---------------------------
# Modal.com
# Run `modal token new` to get your token.
# ---------------------------
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
RENDER_MODE=modal

# ---------------------------
# API Server
# ---------------------------
API_HOST=0.0.0.0
API_PORT=8000
